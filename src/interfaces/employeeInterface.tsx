export interface employeeTime {
    start: string;
    end: string;
}

export interface employeeName {
    first: string;
    last: string;
    reorder: boolean;
}

export interface employeeMeetings {
    when: string;
    onDay: string;
}
